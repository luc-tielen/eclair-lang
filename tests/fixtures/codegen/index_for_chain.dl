@def a(u32, u32, u32, u32, u32).
@def b(u32).


a(1,2,3,4,5).
b(1).

b(x) :-
  // [0,1]
  a(123, 123, x, y, z).

b(x) :-
  // [4,2,3]
  a(x, y, 123, 123, 123).

b(x) :-
  // [4] => [4,2,3]
  a(x, y, z, a, 123).

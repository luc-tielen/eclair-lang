fact_a(1).
fact_b(1, 2).
fact_c(1, 2, 3).
fact_d(1, 2).
triple(1, 2, 3).

a(z) :-
  fact_a(x),
  triple(x, y, z).

b(x, y) :-
  fact_b(x, y),
  triple(x, y, z).

c(x, y, z) :-
  fact_c(x, y, z),
  triple(x, y, z).

d(x, z) :-
  fact_d(x, z),
  triple(x, y, z).
